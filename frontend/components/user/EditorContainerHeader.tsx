import React from 'react';
import styles from '../../styles/User.module.scss';

export const EditorContainerHeader = () => {
    return(
        <div className={styles['editor-header']}>
            Customize
        </div>
    )
}